<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>The Itinerary Hub</title>
    <link rel="stylesheet" type="text/css" href="/stylesheets/application.css" />
    <link href="https://fonts.googleapis.com/css?family=Roboto:500&display=swap" rel="stylesheet">
  </head>
    <body>

    <div class="wrapper">
      <% if flash.has?(:message) %>
        <div class="alert alert-primary" role="alert">
          <%= flash[:message] %>
        </div>
      <% end %>
    </div>

      <div class="container">
        <h1>Welcome to the Itinerary Hub!</h1>
      </div>

      <nav>
        <% if logged_in? %>
        <h4>Welcome, <%= current_user.username %></h4>
            <a href="/users">Home</a><br>
            <a href="/itineraries/new">Create an Itinerary</a><br>
            <a href="/itineraries">Access All Itineraries</a><br>
            <a href="/logout">Log Out</a>
      </nav>

      <% end %>
      <%= yield %>
  </body>
</html>
