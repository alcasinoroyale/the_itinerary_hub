<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>The Itinerary Hub</title>
    <link rel="stylesheet" type="text/css" href="<%= url("/css/style.css")%>">
    <link href="https://fonts.googleapis.com/css?family=Roboto|Avenir+Next|Avenir&display=swap" rel="stylesheet">
    <script src='https://kit.fontawesome.com/a076d05399.js'></script>
  </head>
    <body>

    <div class="header">
      <div class="title">
        <h1><a href="/">The Itinerary Hub</a></h1>
      </div>
        <div class="navigation" id="myNavMenu">
          <% if logged_in? %>
              <a href="javascript:void(0);" class="icon" onclick="responsiveMenu()">
                <i class="fa fa-bars" style="font-size:24px"></i>
              </a>
              <a href="/users/<%= current_user.slug %>"> <i class="fas fa-user"></i> <%= current_user.username %></a>
              <a href="/users">Users</a>
              <a href="/itineraries/new">Create Itinerary</a>
              <a href="/itineraries">Itineraries</a>
              <a href="/logout">Log Out</a>
          <% end %>
        </div>
      </div>

      <script>
        function responsiveMenu() {
          let x = document.getElementById("myNavMenu");
          if (x.className === "navigation") {
            x.className += " responsive";
          } else {
            x.className = "navigation";
          }
        }
      </script>

    <div class="content">
        <%= yield %>
    </div>

    <div class="wrapper">
      <% if flash.has?(:message) %>
        <div class="alert alert-primary" role="alert">
          <%= flash[:message] %>
        </div>
      <% end %>
    </div>

  </body>
</html>
